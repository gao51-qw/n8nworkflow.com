---
/**
 * WorkflowDetailedView 组件
 * 路径: src/components/workflow/WorkflowDetailedView.astro
 * 
 * 用途: 展示工作流详情页的结构化内容（包括免责声明、概览、逻辑块、节点分析）
 * 优先级: P0
 * 
 * Props 接口:
 * - workflow: WorkflowDetailed - 完整的工作流详情对象
 * 
 * 特性:
 * - 分层展示: Disclaimer → Overview → LogicalBlocks → NodeDetails → Related
 * - 每个区域可独立展开/收起
 * - 固定浮动目录导航
 * - 打印友好
 * 
 * 布局: 2列（60%内容 + 40%侧边栏）
 */

import type { WorkflowDetailed } from '../../lib/types/workflow';

interface Props {
  workflow: WorkflowDetailed;
}

const { workflow } = Astro.props;
---

<article class="max-w-7xl mx-auto px-4 md:px-6 py-8">
  <div class="grid grid-cols-1 lg:grid-cols-12 gap-8">
    <!-- 主内容区 (8列) -->
    <div class="lg:col-span-8 space-y-8">
      <!-- 免责声明区域 (如果存在) -->
      {
        workflow.disclaimer && (
          <section class="border-l-4 border-red-600 bg-red-50 dark:bg-red-900/20 p-4 rounded-r">
            <h2 class="text-sm font-bold text-red-900 dark:text-red-200 flex items-center gap-2 mb-2">
              <span>⚠️</span>
              <span>免责声明</span>
            </h2>
            <p class="text-sm text-red-800 dark:text-red-200">{workflow.disclaimer}</p>
          </section>
        )
      }

      <!-- 1. Workflow Overview 部分 -->
      {
        workflow.overview && (
          <section id="overview" class="space-y-6 bg-gradient-to-br from-primary-50 to-primary-50/50 dark:from-gray-800 dark:to-gray-900 border border-primary-200 dark:border-gray-700 rounded-lg p-6">
            <div>
              <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-2">工作流概览</h2>
              <p class="text-gray-700 dark:text-gray-300 leading-relaxed">{workflow.overview.description}</p>
            </div>

            {workflow.overview.features && workflow.overview.features.length > 0 && (
              <div class="border-t border-primary-200 dark:border-gray-600 pt-4">
                <h3 class="font-semibold text-gray-900 dark:text-white mb-3 flex items-center gap-2">
                  <span class="text-lg">✨</span> 关键特性
                </h3>
                <ul class="space-y-2">
                  {workflow.overview.features.map((feature) => (
                    <li class="flex items-start gap-3 text-gray-700 dark:text-gray-300">
                      <span class="text-primary-600 dark:text-primary-400 font-bold mt-0.5">•</span>
                      <span>{feature}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}

            {workflow.overview.useCases && workflow.overview.useCases.length > 0 && (
              <div class="border-t border-primary-200 dark:border-gray-600 pt-4">
                <h3 class="font-semibold text-gray-900 dark:text-white mb-3 flex items-center gap-2">
                  <span class="text-lg">🎯</span> 使用场景
                </h3>
                <ul class="space-y-2">
                  {workflow.overview.useCases.map((useCase) => (
                    <li class="flex items-start gap-3 text-gray-700 dark:text-gray-300">
                      <span class="text-primary-600 dark:text-primary-400 font-bold mt-0.5">→</span>
                      <span>{useCase}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}
          </section>
        )
      }

      <!-- 逻辑块列表部分 -->
      {
        workflow.logicalBlocks && workflow.logicalBlocks.length > 0 && (
          <section id="logical-blocks" class="space-y-4">
            <h3 class="text-xl font-bold text-gray-900 dark:text-white flex items-center gap-2">
              <span class="text-primary-600 dark:text-primary-400">🔗</span> 工作流结构
            </h3>
            <div class="space-y-3">
              {workflow.logicalBlocks.map((block, idx) => (
                <div class="relative bg-white dark:bg-gray-800 p-4 rounded-lg border border-gray-200 dark:border-gray-700 hover:shadow-md transition-shadow">
                  {idx < workflow.logicalBlocks.length - 1 && (
                    <div class="absolute left-6 top-full h-3 w-0.5 bg-gradient-to-b from-primary-300 to-transparent"></div>
                  )}
                  <div class="flex items-start gap-3">
                    <span class="flex-shrink-0 w-6 h-6 rounded-full bg-primary-600 dark:bg-primary-500 text-white text-xs font-bold flex items-center justify-center mt-0.5">
                      {block.order}
                    </span>
                    <div class="flex-1">
                      <h4 class="font-semibold text-gray-900 dark:text-white">{block.title}</h4>
                      <p class="text-sm text-gray-600 dark:text-gray-400 mt-1">{block.description}</p>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          </section>
        )
      }

      <!-- 2. Block-by-Block Analysis 部分 -->
      {
        workflow.nodeDetails && workflow.nodeDetails.length > 0 && (
          <section id="block-analysis" class="space-y-4">
            <h2 class="text-2xl font-bold text-gray-900 dark:text-white flex items-center gap-2">
              <span class="text-primary-600 dark:text-primary-400">⚙️</span> 节点详细分析
            </h2>

            <div class="space-y-6">
              {workflow.nodeDetails.map((node, index) => (
                <div class="border border-gray-200 dark:border-gray-600 rounded-lg p-6 bg-white dark:bg-gray-800">
                  <h3 class="text-lg font-bold text-gray-900 dark:text-white mb-4">
                    节点 {index + 1}: {node.name}
                  </h3>

                  <div class="space-y-3 text-sm">
                    <div>
                      <span class="font-semibold text-gray-900 dark:text-white">类型/角色:</span>
                      <p class="text-gray-700 dark:text-gray-300">
                        {node.type} - {node.role}
                      </p>
                    </div>

                    {node.configDescription && (
                      <div>
                        <span class="font-semibold text-gray-900 dark:text-white">配置说明:</span>
                        <p class="text-gray-700 dark:text-gray-300">{node.configDescription}</p>
                      </div>
                    )}

                    {node.connections && node.connections.length > 0 && (
                      <div>
                        <span class="font-semibold text-gray-900 dark:text-white">连接:</span>
                        <p class="text-gray-700 dark:text-gray-300">{node.connections.join(', ')}</p>
                      </div>
                    )}

                    {node.edgeCases && (
                      <div>
                        <span class="font-semibold text-gray-900 dark:text-white">边界情况:</span>
                        <p class="text-gray-700 dark:text-gray-300">{node.edgeCases}</p>
                      </div>
                    )}
                  </div>
                </div>
              ))}
            </div>
          </section>
        )
      }
    </div>

    <!-- 右侧边栏 (4列) -->
    <aside class="lg:col-span-4 space-y-6">
      <!-- 工作流预览/查看器 -->
      <div class="bg-gray-50 dark:bg-gray-800 rounded-lg p-6">
        <h3 class="font-bold text-gray-900 dark:text-white mb-4">工作流预览</h3>
        <img
          src={workflow.thumbnail || '/placeholder-workflow.svg'}
          alt={`${workflow.title} 预览`}
          class="w-full h-auto rounded border border-gray-200 dark:border-gray-700"
        />
      </div>

      <!-- 作者信息卡片 -->
      <div class="bg-gradient-to-br from-blue-50 to-blue-50/50 dark:from-gray-800 dark:to-gray-900 rounded-lg p-6 border border-blue-200 dark:border-gray-700">
        <h3 class="font-bold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
          <span class="text-lg">👤</span> 作者
        </h3>
        <div class="flex items-start gap-4">
          <div class="w-14 h-14 bg-gradient-to-br from-primary-500 to-primary-600 rounded-full flex items-center justify-center text-white font-bold text-lg flex-shrink-0">
            {workflow.author.name.charAt(0)}
          </div>
          <div class="flex-1 min-w-0">
            <p class="font-semibold text-gray-900 dark:text-white truncate">{workflow.author.name}</p>
            <p class="text-sm text-gray-600 dark:text-gray-400 line-clamp-2">{workflow.author.bio}</p>
          </div>
        </div>
      </div>

      <!-- 统计信息面板 -->
      <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
        <h3 class="font-bold text-gray-900 dark:text-white mb-4 flex items-center gap-2">
          <span class="text-lg">📊</span> 统计信息
        </h3>
        <div class="grid grid-cols-2 gap-4">
          <div class="bg-gradient-to-br from-blue-50 to-blue-50/50 dark:from-gray-700 dark:to-gray-800 rounded-lg p-3 border border-blue-100 dark:border-gray-600">
            <p class="text-xs text-gray-600 dark:text-gray-400 font-medium">👁️ 浏览</p>
            <p class="text-xl font-bold text-gray-900 dark:text-white mt-1">{workflow.visitors || 0}</p>
          </div>
          <div class="bg-gradient-to-br from-green-50 to-green-50/50 dark:from-gray-700 dark:to-gray-800 rounded-lg p-3 border border-green-100 dark:border-gray-600">
            <p class="text-xs text-gray-600 dark:text-gray-400 font-medium">⬇️ 下载</p>
            <p class="text-xl font-bold text-gray-900 dark:text-white mt-1">{workflow.downloads || 0}</p>
          </div>
          <div class="bg-gradient-to-br from-red-50 to-red-50/50 dark:from-gray-700 dark:to-gray-800 rounded-lg p-3 border border-red-100 dark:border-gray-600">
            <p class="text-xs text-gray-600 dark:text-gray-400 font-medium">❤️ 点赞</p>
            <p class="text-xl font-bold text-gray-900 dark:text-white mt-1">{workflow.likes || 0}</p>
          </div>
          <div class="bg-gradient-to-br from-purple-50 to-purple-50/50 dark:from-gray-700 dark:to-gray-800 rounded-lg p-3 border border-purple-100 dark:border-gray-600">
            <p class="text-xs text-gray-600 dark:text-gray-400 font-medium">🎯 难度</p>
            <p class="text-xl font-bold text-gray-900 dark:text-white mt-1 capitalize">{workflow.complexityLevel}</p>
          </div>
        </div>
      </div>

      <!-- 操作按钮组 -->
      <div class="space-y-2">
        <button class="w-full px-4 py-3 bg-gradient-to-r from-primary-600 to-primary-700 text-white rounded-lg font-semibold hover:from-primary-700 hover:to-primary-800 transition-all shadow-md hover:shadow-lg">
          ⬇️ 下载工作流
        </button>
        <button class="w-full px-4 py-3 border-2 border-gray-300 dark:border-gray-600 text-gray-700 dark:text-gray-300 rounded-lg font-semibold hover:bg-gray-50 dark:hover:bg-gray-700 transition-all hover:border-gray-400 dark:hover:border-gray-500">
          📋 复制配置
        </button>
        <button class="w-full px-4 py-3 border-2 border-green-300 dark:border-green-600 text-green-700 dark:text-green-300 rounded-lg font-semibold hover:bg-green-50 dark:hover:bg-green-900/20 transition-all hover:border-green-400 dark:hover:border-green-500">
          💙 分享
        </button>
      </div>
    </aside>
  </div>
</article>

<style>
  @media print {
    aside {
      display: none;
    }
  }
</style>
